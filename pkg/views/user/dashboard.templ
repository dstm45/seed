package user

import "github.com/dstm45/seed/pkg/views/base"

templ Dashboard(username string) {
    @base.Base("Dashboard") {
        <div>
                Dashboard de {username}
        </div>
    }
}